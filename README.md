# D3 Bar Chart

Interactive horizontal bar chart built with React, Vite, and D3.

Displays students per million by country. D3 handles scale math; React renders the SVG declaratively.

## Development

```bash
npm run dev      # Start dev server
npm run build    # Production build
npm run preview  # Preview production build
```
